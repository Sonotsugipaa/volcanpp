# CMake build directories, as created by ./build.sh
default/
debug/
release/
release-debug/
release-size/

# Any hidden file
.*/
.*

# Locally stored external dependencies
include/
lib/
bin/

# params.cfg is created at runtime if it doesn't exist, but
# a local default can be handy for quick testing/debugging
src/params.cfg

# Assets are to be stored separatedly from the repository
src/assets/*
!src/assets/CMakeLists.txt

# Various directories contain .doc.md files for documentation
!**/*.doc.md

# Runtime files generated by vkconfig
gfxrecon_capture.gfxr
vk_apidump.txt
